services:
  # ==========================================
  # DATABASES
  # ==========================================
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Login@321
      - POSTGRES_DB=ecommerce
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network
    ports:
      - "5432:5432"

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data
    networks:
      - app_network
    ports:
      - "6379:6379"

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    restart: always
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m" 
    volumes:
      - es_data:/usr/share/elasticsearch/data
    networks:
      - app_network

  # ==========================================
  # BACKEND
  # ==========================================
  backend:
    build: .
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_NAME=ecommerce
      - DB_PASSWORD=Login@321
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ES_NODE_URL=http://elasticsearch:9200
    depends_on:
      - postgres
      - redis
      - elasticsearch
    networks:
      - app_network

volumes:
  postgres_data:
  redis_data:
  es_data:

networks:
  app_network:
    driver: bridge
